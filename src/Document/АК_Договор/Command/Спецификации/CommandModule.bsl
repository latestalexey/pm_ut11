
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	СоставДокументов = Новый Массив;
	Для Каждого СтрТЗ ИЗ ПараметрыВыполненияКоманды.Источник.Объект.Спецификации Цикл
		Если СтрТЗ.Печатать Тогда
			СоставДокументов.Добавить(СтрТЗ.Документ);
		КонецЕсли;	
	КонецЦикла;	
	//ПараметрКоманды= СоставДокументов;
	Для Каждого ДокументСсылка Из СоставДокументов Цикл
        ДокументИзСостава = Новый Массив;
        ДокументИзСостава.Добавить(ДокументСсылка);
		УправлениеПечатьюКлиент.ВыполнитьКомандуПечати(
			"Документ.АК_Договор_Спецификация",
			"АК_Спецификация",
			ДокументИзСостава,
			Неопределено,
			Неопределено
		);
    КонецЦикла;
		
КонецПроцедуры
