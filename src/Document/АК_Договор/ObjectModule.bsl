Процедура ЗаполнитьСпецификацииСервер() Экспорт
	Запрос=Новый Запрос("ВЫБРАТЬ
	                    |	АК_Договор_Спецификация.Ссылка КАК Документ
	                    |ИЗ
	                    |	Документ.АК_СпецификацииДоговоров КАК АК_Договор_Спецификация
	                    |ГДЕ
	                    |	АК_Договор_Спецификация.ДокументОснование = &ДокументОснование");
	Запрос.УстановитьПараметр("ДокументОснование",ЭтотОбъект.Ссылка);
	Если  ЭтотОбъект.Спецификации.Количество()<>Запрос.Выполнить().Выгрузить().Количество() Тогда
		ЭтотОбъект.Спецификации.загрузить(Запрос.Выполнить().Выгрузить());
	КонецЕсли;	
КонецПроцедуры;	