#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда


Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	ТекстСообщения = "";
	ПолеНоменклатура = КомпоновщикНастроек.ФиксированныеНастройки.ПараметрыДанных.Элементы.Найти("Номенклатура");
	Если ПолеНоменклатура = Неопределено ИЛИ НЕ ПолеНоменклатура.Использование Тогда
		ТекстСообщения = НСтр("ru='Не задано значение параметра ""Номенклатура"". Отчет может быть вызван только из формы элемента справочника ""Номенклатура""'");
	КонецЕсли;
	Если НЕ ПустаяСтрока(ТекстСообщения) Тогда
		СтандартнаяОбработка = Ложь;
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
	КонецЕсли;
КонецПроцедуры

#КонецЕсли