#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда


///////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ЗначениеЗаполнено(Документ) Тогда
		
		Если ТипЗнч(Документ) = Тип("ДокументСсылка.ПередачаТоваровМеждуОрганизациями") Тогда
			ПоТоварамКОформлению = Документы.ПередачаТоваровМеждуОрганизациями.ПолучитьРеквизитыДокумента(Документ).ПоТоварамКОформлению;
		ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.ВозвратТоваровМеждуОрганизациями") Тогда
			ПоТоварамКОформлению = Документы.ВозвратТоваровМеждуОрганизациями.ПолучитьРеквизитыДокумента(Документ).ПоТоварамКОформлению;
		Иначе	
			ПоТоварамКОформлению = Истина;
		КонецЕсли;
		
		Если Не ПоТоварамКОформлению Тогда
			Текст = НСтр("ru = 'Документ не заполнен ""По товарам к оформлению"". Отчет формировать не требуется.'");
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
				Текст,
				Документ,
				"ПоТоварамКОформлению",
				,
				Отказ
			);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры


#КонецЕсли