#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

///////////////////////////////////////////////////////////////////////////////
// ПРОГРАММНЫЙ ИНТЕРФЕЙС

// СтандартныеПодсистемы.ВариантыОтчетов

// Методы расширения общей формы отчетов
//

// Вызывается в одноименном обработчике формы отчета после выполнения кода формы.
//
// Подробнее - см. ФормаОтчетаПереопределяемый.ПриСозданииНаСервере
//
Процедура ПриСозданииНаСервере(ЭтаФорма, Отказ, СтандартнаяОбработка) Экспорт
	// Дополнительные команды
	Элементы = ЭтаФорма.Элементы;
	Команды = ЭтаФорма.Команды;
	ПостоянныеКоманды = ЭтаФорма.ПостоянныеКоманды;
	
	// Создание команды
	КомандаАктуализироватьРасчеты = Команды.Добавить("АктуализироватьДанныеПартионногоУчета");
	КомандаАктуализироватьРасчеты.Действие = "Подключаемый_Команда";
	КомандаАктуализироватьРасчеты.Заголовок = НСтр("ru= 'Актуализировать данные'");
	КомандаАктуализироватьРасчеты.Подсказка = НСтр("ru= 'Актуализировать данные партионного учета.'");
	
	ПостоянныеКоманды.Добавить("АктуализироватьДанныеПартионногоУчета");
	
	// Добавление кнопки вызова команды
	Кнопка = Элементы.Вставить("АктуализироватьДанныеПартионногоУчета", Тип("КнопкаФормы"), Элементы.ЛеваяЧастьКоманднойПанели, Элементы.КнопкиТабличногоДокумента);
	Кнопка.ИмяКоманды = "АктуализироватьДанныеПартионногоУчета";
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.ВариантыОтчетов
#КонецЕсли