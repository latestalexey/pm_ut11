
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если НЕ ТипЗнч(ПараметрКоманды) = Тип("Массив") ИЛИ ПараметрКоманды.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	// подписать (если надо) и отправить подтверждение
	ОбменЭДКлиент.ОбработатьЭД(ПараметрКоманды, ПараметрыВыполненияКоманды.Источник.УникальныйИдентификатор, "ОтправитьПодтверждениеЭД");
	
	Оповестить("ОбновитьСостояниеВерсииЭД");
	
КонецПроцедуры