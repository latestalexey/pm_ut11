
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
		
	УправлениеПечатьюКлиент.ВыполнитьКомандуПечати(
		"Обработка.ПечатьСчетовНаОплату",
		"СчетНаОплату",
		ПолучитьДокумент(ПараметрКоманды),
		Неопределено,
		Неопределено
	);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьДокумент(ПараметрКоманды)
	
	МассивЗаказов = Новый Массив;
	Для каждого ТекСтрока Из ПараметрКоманды Цикл
		МассивЗаказов.Добавить(ТекСтрока.ЗаказКлиента);
	КонецЦикла;  
	Возврат МассивЗаказов;
	
КонецФункции